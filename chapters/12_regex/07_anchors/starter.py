# Puzzle 12.7 -- Anchors: match the whole string
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define is_valid_code(text) that RETURNS True only when
#       text is exactly two uppercase letters then four digits (e.g. "AB1234"),
#       using re.fullmatch (or ^...$).
#
#     is_valid_code("AB1234")  -> True
#     is_valid_code("AB1234x") -> False

# TODO: your code here
