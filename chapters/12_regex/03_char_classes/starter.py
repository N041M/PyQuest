# Puzzle 12.3 -- Character classes: [aeiou]
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define count_vowels(text) that RETURNS how many vowels
#       are in text, using re.findall with a [aeiou] character class.
#
#     count_vowels("education") -> 5
#     count_vowels("xyz")       -> 0

# TODO: your code here
