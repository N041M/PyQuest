# Puzzle 12.6 -- re.sub: find and replace by pattern
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define redact(text) that replaces every run of digits
#       with a single "#", using re.sub(r"\d+", "#", text).
#
#     redact("call 555-1234") -> "call #-#"
#     redact("no digits")     -> "no digits"

# TODO: your code here
