# Puzzle 12.5 -- Groups: capture the parts
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define parse_date(text) that takes "YYYY-MM-DD" and
#       RETURNS the tuple of integers (year, month, day), using capture groups.
#
#     parse_date("2026-06-20") -> (2026, 6, 20)
#     parse_date("1999-01-05") -> (1999, 1, 5)
#
# re.match(r"(\d+)-(\d+)-(\d+)", text) captures the three parts.

# TODO: your code here
