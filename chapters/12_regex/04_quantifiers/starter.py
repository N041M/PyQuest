# Puzzle 12.4 -- Quantifiers: + means one or more
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define find_words(text) that RETURNS a list of the words
#       in text -- runs of one or more letters -- using re.findall(r"[A-Za-z]+").
#
#     find_words("Hello, world!")  -> ["Hello", "world"]
#     find_words("one-two three")  -> ["one", "two", "three"]

# TODO: your code here
