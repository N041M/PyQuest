# Puzzle 12.2 -- re.findall: every match
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define all_numbers(text) that RETURNS a list of every
#       run of digits in text, as strings, using re.findall with \d+.
#
#     all_numbers("a12b3c456") -> ["12", "3", "456"]
#     all_numbers("nothing")   -> []

# TODO: your code here
