# Puzzle 12.1 -- re.search: is the pattern there?
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import re and define has_digit(text) that RETURNS True if text
#       contains at least one digit, using re.search with \d.
#
#     has_digit("abc4") -> True
#     has_digit("abc")  -> False
#
# re.search returns a match object or None; compare to None for a bool.

# TODO: your code here
