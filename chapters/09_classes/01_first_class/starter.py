# Puzzle 9.1 -- A first class
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a class Dog whose __init__ takes name and age and stores each
#   on the object (self.name, self.age).
#
#     Dog("Rex", 3) -> .name == "Rex", .age == 3
#
# Shape: class Dog: -> def __init__(self, name, age): -> self.name = name ...
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
