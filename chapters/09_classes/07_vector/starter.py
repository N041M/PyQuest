# Puzzle 9.7 -- Objects working together
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a class Vector storing x and y, with a method add(other) that
#   returns a NEW Vector summing the two vectors' coordinates. Leave the
#   originals unchanged.
#
#     Vector(1, 2).add(Vector(3, 4)) -> Vector with .x == 4, .y == 6
#
# Shape: def add(self, other): return Vector(self.x + other.x, self.y + other.y)
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
