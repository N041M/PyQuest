# Puzzle 9.3 -- State that remembers
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a class Counter starting count at 0, with a method tick() that
#   adds one and RETURNS the new count.
#
#     c = Counter(); c.tick(); c.tick() -> 1 then 2
#
# Shape: __init__ sets self.count = 0 ; tick() does self.count += 1; return it
#   The count must live on self, so two counters stay independent.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
