# Puzzle 9.8 -- Capstone: a bank account
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a class BankAccount with balance (default 0), deposit(amount),
#   and withdraw(amount). withdraw subtracts and returns True only if there is
#   enough; otherwise it changes nothing and returns False.
#
#     BankAccount(100); deposit(50) -> 150; withdraw(70) -> True, 80;
#                                            withdraw(999) -> False, still 80
#
# Shape: __init__(self, balance=0); deposit += ; withdraw guards with an if.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
