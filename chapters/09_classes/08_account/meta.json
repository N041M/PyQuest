{
  "id": "9.8",
  "title": "Capstone: a bank account",
  "chapter": "Classes & Objects",
  "concept": "a stateful class with a default, mutating methods, and a guarded rule the object enforces",
  "mode": "import",
  "why": "balance is per-instance state that deposit/withdraw update; withdraw's if-guard refuses an overdraw and reports it via True/False, so the object protects its own invariant."
}
