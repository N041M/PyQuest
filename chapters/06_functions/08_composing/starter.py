# Puzzle 6.8 -- Functions calling functions
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define BOTH functions:
#   clean(text)     -> text stripped of surrounding spaces and lowercased
#   same_word(a, b) -> True when a and b are the same after cleaning
#
#     clean("  Tea ")            -> "tea"
#     same_word("  Tea ", "tea") -> True
#
# same_word must CALL clean -- don't repeat the recipe.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
