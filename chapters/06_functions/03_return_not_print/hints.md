If the checker says it got None, your function printed instead of returning.

---

Build the string with .upper() and + "!", then return it -- no print anywhere.

---

def shout(word):
    return word.upper() + "!"
