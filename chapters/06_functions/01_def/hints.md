The shape is:  def name(parameter):  then an indented body that returns
something.

---

`def double(x):` on the first line; the body is one line: return x times 2.

---

def double(x):
    return x * 2
