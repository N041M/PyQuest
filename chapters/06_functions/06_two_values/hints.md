Two values after one return, separated by a comma, come back as a tuple.

---

You already have both halves from 5.3: `return min(nums), max(nums)`.

---

def min_max(nums):
    return min(nums), max(nums)
