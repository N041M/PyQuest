# Puzzle 6.9 -- Recursion: a function calling itself
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define fact(n) that returns n! RECURSIVELY.
#   n! = n * (n-1) * ... * 1, and 0! = 1. n is never negative.
#
#     fact(0) -> 1
#     fact(5) -> 120
#
# Two parts: a base case (n == 0), and  n * fact(n - 1).
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
