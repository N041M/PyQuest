An average is the total divided by the count -- and you have a built-in for
each half.

---

`sum(nums)` over `len(nums)`, with `/` (the float division from 1.9).

---

def average(nums):
    return sum(nums) / len(nums)
