# Puzzle 6.7 -- Building on built-ins
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define average(nums) returning the average of a non-empty list.
#
#     average([1, 2])       -> 1.5
#     average([10, 20, 30]) -> 20.0
#
# sum() and len() do the heavy lifting; / keeps it a float.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
