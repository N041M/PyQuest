# Puzzle 15.7 -- __lt__: make objects sortable
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: Temperature.__init__(self, degrees). __lt__(self, other) so they
#       compare by degrees, making a list of them sortable.
#
#     Temperature(10) < Temperature(20)  -> True
#     sorted([T(30), T(10), T(20)])      -> 10, 20, 30

# TODO: your code here
