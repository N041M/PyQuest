# Puzzle 15.4 -- @property: a computed attribute
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: Rectangle.__init__(self, width, height); an `area` PROPERTY returning
#       width * height (computed on access, not stored).
#
#     Rectangle(3, 4).area -> 12       (no parentheses)
#     after r.width = 5,  r.area -> 20

# TODO: your code here
