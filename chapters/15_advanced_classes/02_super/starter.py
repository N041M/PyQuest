# Puzzle 15.2 -- super(): extend the parent
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: Animal.__init__(self, name) stores self.name. Dog(Animal).__init__(self,
#       name, breed) calls super().__init__(name), then stores self.breed.
#
#     Dog("Rex", "Lab").name  -> "Rex"
#     Dog("Rex", "Lab").breed -> "Lab"

# TODO: your code here
