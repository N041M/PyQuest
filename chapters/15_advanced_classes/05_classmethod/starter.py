# Puzzle 15.5 -- @classmethod: an alternative constructor
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: Point.__init__(self, x, y). A @classmethod from_tuple(cls, pair) that
#       builds a Point from a (x, y) tuple using cls(...).
#
#     Point.from_tuple((3, 4)).x -> 3
#     Point.from_tuple((3, 4)).y -> 4

# TODO: your code here
