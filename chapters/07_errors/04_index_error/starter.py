# Puzzle 7.4 -- IndexError and safe access
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define item_or(items, i, default) -> items[i], or default when i
#   is out of range. Negative indexes that fit are VALID (items[-1] = last).
#
#     item_or(["a", "b"], 0, "?")  -> "a"
#     item_or(["a", "b"], -1, "?") -> "b"
#     item_or(["a", "b"], 5, "?")  -> "?"
#
# Use try/except IndexError.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
