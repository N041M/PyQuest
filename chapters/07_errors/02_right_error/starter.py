# Puzzle 7.2 -- Catch the RIGHT error
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define safe_int(text) -> int(text), or None if the text is not a
#   valid whole number.
#
#     safe_int("42")   -> 42
#     safe_int("nope") -> None
#     safe_int([1, 2]) -> TypeError  (must NOT be caught -- that's a misuse)
#
# Catch ONLY ValueError. A bare except will fail the check.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
