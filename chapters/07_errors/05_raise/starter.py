# Puzzle 7.5 -- raise: errors are yours too
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define checked_age(age) that returns age unchanged, but raises a
#   ValueError when age is negative.
#
#     checked_age(30) -> 30
#     checked_age(0)  -> 0
#     checked_age(-1) -> ValueError
#
# raise ValueError("a message saying what's wrong")
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
