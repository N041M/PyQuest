# Puzzle 10.2 -- yield inside a loop
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a generator squares(n) that YIELDS 0, 1, 4, 9, ...
#       (the squares of 0 up to but not including n). n <= 0 yields nothing.
#
#     list(squares(4)) -> [0, 1, 4, 9]
#     list(squares(0)) -> []
#
# Put yield inside a loop -- one yield per number.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
