# Puzzle 10.4 -- a generator remembers
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a generator running_total(nums) that YIELDS the running sum:
#       each value is the total so far. An empty list yields nothing.
#
#     list(running_total([3, 1, 2])) -> [3, 4, 6]
#     list(running_total([]))        -> []
#
# Keep a total in a variable; it survives across the yields.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
