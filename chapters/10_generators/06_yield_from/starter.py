# Puzzle 10.6 -- yield from: pass a whole stream along
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a generator chain(a, b) that YIELDS all items of a, then all
#       items of b, in order. Use yield from. Either list may be empty.
#
#     list(chain([1, 2], [3, 4])) -> [1, 2, 3, 4]
#     list(chain([], [9]))        -> [9]
#
#     yield from a   # re-emits every item of a, one by one
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
