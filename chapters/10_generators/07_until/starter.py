# Puzzle 10.7 -- stop early
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a generator until_zero(nums) that YIELDS each number until it
#       reaches a 0, then stops. The 0 and anything after it are not yielded.
#       No 0 at all -> yield the whole list.
#
#     list(until_zero([1, 2, 0, 3])) -> [1, 2]
#     list(until_zero([0, 9]))       -> []
#
# Use yield, and stop early (a bare `return`) when you hit a 0.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
