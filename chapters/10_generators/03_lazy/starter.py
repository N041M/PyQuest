# Puzzle 10.3 -- generators are lazy
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define an ENDLESS generator naturals() that YIELDS 0, 1, 2, 3, ...
#       forever. It must never stop on its own.
#
#     first 5 values -> [0, 1, 2, 3, 4]
#
# A while True loop is fine here: yield pauses it until more is asked for.
# The checker only ever pulls a handful of values, so it will not hang.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
