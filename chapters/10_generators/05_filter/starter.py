# Puzzle 10.5 -- filter while you yield
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a generator evens(nums) that YIELDS only the even numbers,
#       in their original order. (n is even when n % 2 == 0.)
#
#     list(evens([1, 2, 3, 4])) -> [2, 4]
#     list(evens([1, 3, 5]))    -> []
#
# Put the yield behind an if, so only the even numbers come out.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
