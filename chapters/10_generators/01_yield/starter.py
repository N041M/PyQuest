# Puzzle 10.1 -- yield: a function that pauses
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define a generator count_down(n) that YIELDS n, n-1, ..., 1.
#       If n <= 0 it yields nothing.
#
#     list(count_down(5)) -> [5, 4, 3, 2, 1]
#     list(count_down(0)) -> []
#
# Use yield (not return of a list): calling count_down should give a generator.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
