# Puzzle 11.8 -- Capstone: a stats summary from JSON
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define summary(numbers_json) that parses a JSON string of numbers and
#       RETURNS a dict: count, mean, max, min.
#
#     summary("[2, 4, 6]") -> {"count": 3, "mean": 4, "max": 6, "min": 2}
#
# Parse with json.loads; take the mean from statistics; max/min are built in.

# TODO: your code here
