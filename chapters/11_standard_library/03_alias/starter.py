# Puzzle 11.3 -- import as: rename on the way in
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: with `import statistics as stats`, define average(nums) that RETURNS
#       the mean of the list, using stats.mean.
#
#     average([2, 4, 6]) -> 4
#     average([1, 2])    -> 1.5
#
# The alias `stats` is the name you reach the module through.

# TODO: your code here
