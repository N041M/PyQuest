# Puzzle 11.5 -- Counter: tally in one step
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: with `from collections import Counter`, define tally(items) that
#       RETURNS a count of each distinct item in the list.
#
#     tally(["a", "b", "a"]) -> {"a": 2, "b": 1}
#     tally([])              -> {}
#
# Let Counter do the counting -- no manual loop.

# TODO: your code here
