# Puzzle 11.1 -- import: bring in a module
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import the math module and define hypotenuse(a, b) that RETURNS
#       the square root of a*a + b*b, using math.sqrt.
#
#     hypotenuse(3, 4)  -> 5.0
#     hypotenuse(5, 12) -> 13.0
#
# The import goes at the top of the file, on its own line.

# TODO: your code here
