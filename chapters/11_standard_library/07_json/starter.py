# Puzzle 11.7 -- json: data as text
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: import json and define to_json(record) that RETURNS the JSON string
#       for the dictionary record, using json.dumps.
#
#     to_json({"a": 1, "b": 2}) -> '{"a": 1, "b": 2}'
#     to_json({})               -> '{}'

# TODO: your code here
