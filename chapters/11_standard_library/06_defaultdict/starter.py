# Puzzle 11.6 -- defaultdict: a default for missing keys
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: with `from collections import defaultdict`, define
#       group_by_length(words) that RETURNS a dict of word length -> list of
#       words of that length, in their original order.
#
#     group_by_length(["hi", "ok", "bye"]) -> {2: ["hi", "ok"], 3: ["bye"]}
#
# Use defaultdict(list) so groups[len(w)].append(w) just works.

# TODO: your code here
