# Puzzle 11.2 -- from import: pull out one name
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: with `from math import gcd`, define simplify(num, den) that RETURNS
#       the fraction reduced to lowest terms as a tuple (top, bottom).
#
#     simplify(6, 8)  -> (3, 4)
#     simplify(10, 5) -> (2, 1)
#
# Divide both num and den by gcd(num, den).

# TODO: your code here
