Read both numbers, then swap them in one line with a, b = b, a.

---

`a = int(input())`, `b = int(input())`, then `a, b = b, a`, then print a and b.

---

a = int(input())
b = int(input())
a, b = b, a
print(a)
print(b)
