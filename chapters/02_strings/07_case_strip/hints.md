Methods are called with a dot: s.strip(), s.upper(). They return new strings.

---

Chain them: s.strip() removes the end spaces, .upper() capitalises the result.

---

s = input()
print(s.strip().upper())
