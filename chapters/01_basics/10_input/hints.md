Store what input() returns in a variable, then build the greeting with +.

---

`name = input()` then `print("Hello, " + name)`. The text before the name
includes the comma and a space: "Hello, ".

---

name = input()
print("Hello, " + name)
