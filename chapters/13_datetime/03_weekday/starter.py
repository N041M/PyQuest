# Puzzle 13.3 -- weekday: which day of the week
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define weekday(text) that takes a YYYY-MM-DD string and RETURNS its
#       day of the week as an int (Monday=0 .. Sunday=6), via .weekday().
#
#     weekday("2026-06-20") -> 5   (Saturday)
#     weekday("2000-01-01") -> 5

# TODO: your code here
