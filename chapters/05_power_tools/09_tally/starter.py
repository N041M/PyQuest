# Puzzle 5.9 -- Counting with a dict
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: read a line of words, then a query word.
#   Print how many times the query appears in the line.
#
#     input "tea milk tea", then "tea"   -> 2
#     input "tea milk tea", then "cocoa" -> 0
#
# Build a dict tally:  counts[w] = counts.get(w, 0) + 1
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
