# Puzzle 5.8 -- Filtering with comprehensions
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: read n, then n whole numbers. Keep only the EVEN ones (original
#   order) and print them one per line.
#
#     input 5, then 1, 2, 3, 4, -6 -> 2 / 4 / -6
#
# Use a comprehension with an if:  [x for x in ... if ...]
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
