# Puzzle 14.8 -- Capstone: a ranked shortlist
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define passing(records, threshold) over a list of (name, score) tuples.
#       RETURN the names with score >= threshold, ordered highest score first,
#       built with filter, sorted(key=lambda), and map.
#
#     passing([("Ada", 90), ("Linus", 70), ("Grace", 95)], 80) -> ["Grace", "Ada"]
#     passing([], 50) -> []

# TODO: your code here
