# Puzzle 14.7 -- reduce: fold a sequence to one value
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: using reduce from functools, define product(nums) that RETURNS the
#       product of all the numbers, with a start of 1.
#
#     product([1, 2, 3, 4]) -> 24
#     product([])           -> 1
#
# reduce(lambda a, b: a * b, nums, 1)

# TODO: your code here
