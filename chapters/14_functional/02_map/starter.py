# Puzzle 14.2 -- map: apply to every item
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: using map, define squares(nums) that RETURNS a list of each number
#       squared.
#
#     squares([1, 2, 3]) -> [1, 4, 9]
#     squares([])        -> []
#
# map returns a lazy iterator -- wrap it in list(...).

# TODO: your code here
