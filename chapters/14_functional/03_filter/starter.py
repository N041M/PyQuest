# Puzzle 14.3 -- filter: keep what passes
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: using filter, define evens(nums) that RETURNS only the even numbers.
#
#     evens([1, 2, 3, 4]) -> [2, 4]
#     evens([1, 3, 5])    -> []
#
# filter returns a lazy iterator -- wrap it in list(...).

# TODO: your code here
