# Puzzle 14.4 -- sorted(key=lambda): sort by a derived value
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: using sorted with a key=lambda, define by_last(words) that RETURNS the
#       words sorted by their LAST character.
#
#     by_last(["pear", "fig", "kiwi"]) -> ["fig", "kiwi", "pear"]
#     by_last([])                      -> []

# TODO: your code here
