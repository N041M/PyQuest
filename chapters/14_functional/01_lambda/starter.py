# Puzzle 14.1 -- lambda: a function in an expression
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: define multiplier(n) that RETURNS a lambda multiplying its argument
#       by n.
#
#     multiplier(3)(4)  -> 12
#     multiplier(10)(5) -> 50
#
# Return the lambda directly: lambda x: x * n

# TODO: your code here
