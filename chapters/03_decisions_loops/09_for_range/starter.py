# Puzzle 3.9 -- for and range
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: read a whole number n, then print 0, 1, ..., n-1 (each on its own line)
# using a for loop with range.
#
#     input 4 -> 0 / 1 / 2 / 3   (each on its own line)
#     input 0 -> (no output)
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
