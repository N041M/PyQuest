# Puzzle 3.8 -- Looping until a sentinel
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: read whole numbers one per line and add them up. Stop when 0 is
# entered (don't add the 0). Print the total.
#
#     input 4, 5, 0 -> 9
#     input 0       -> 0
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
