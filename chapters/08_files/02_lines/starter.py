# Puzzle 8.2 -- A file, line by line
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: print every line of tasks.txt, numbered from 1, as "1. text".
#   Strip the trailing newline off each line.
#
#     tasks.txt = wash / cook / sleep  ->  "1. wash" / "2. cook" / "3. sleep"
#
# Shape: with open("tasks.txt") as f: -> for i, line in enumerate(f, 1): ...
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
