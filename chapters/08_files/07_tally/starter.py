# Puzzle 8.7 -- A frequency report
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: count how often each word appears in words.txt; write report.txt with
#   one "word: count" line per distinct word, ordered by count (highest first),
#   ties broken alphabetically.
#
#     words.txt = fig fig pear fig pear  ->  report.txt = "fig: 3" / "pear: 2"
#
# Shape: read words; tally with counts.get(w,0)+1; write sorted by (-count, word)
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
