# Puzzle 8.5 -- Append, don't overwrite
# Read this puzzle's brief.md first (its path is shown by `start`).
#
# TASK: read one line from input() and append it to log.txt as a new line,
#   WITHOUT erasing what is already in the file.
#
#     log.txt = woke up / ate, input "ran"  ->  woke up / ate / ran
#
# Shape: entry = input() -> with open("log.txt", "a") as f: f.write(entry+"\n")
#   "a" appends; "w" would wipe the file first -- that's the lesson.
#
# Write your code in this file, save it, then run:  check

# TODO: your code here
