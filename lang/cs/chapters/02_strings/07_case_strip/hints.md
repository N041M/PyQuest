Metody se volají s tečkou: s.strip(), s.upper(). Vracejí nové řetězce.

---

Zřetěz je: s.strip() odstraní koncové mezery, .upper() převede výsledek na velká
písmena.

---

s = input()
print(s.strip().upper())
