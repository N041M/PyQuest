Začni s `import statistics as stats`. Od té chvíle je `stats` tvoje jméno pro
modul.

---

`stats.mean(nums)` vrátí průměr seznamu. Celá tvá funkce může být jeden řádek,
který ho vrátí.

---

import statistics as stats


def average(nums):
    return stats.mean(nums)
