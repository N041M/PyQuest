Průměr je součet dělený počtem -- a pro každou polovinu máš vestavěnou funkci.

---

`sum(nums)` přes `len(nums)`, pomocí `/` (float dělení z 1.9).

---

def average(nums):
    return sum(nums) / len(nums)
