Ulož to, co vrátí input(), do proměnné a pak sestav pozdrav pomocí +.

---

`name = input()` a pak `print("Hello, " + name)`. Text před jménem obsahuje čárku
a mezeru: „Hello, “.

---

name = input()
print("Hello, " + name)
