Přečti obě čísla, pak je na jednom řádku prohoď pomocí a, b = b, a.

---

`a = int(input())`, `b = int(input())`, pak `a, b = b, a`, pak vypiš a a b.

---

a = int(input())
b = int(input())
a, b = b, a
print(a)
print(b)
