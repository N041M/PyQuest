Começa com uma lista vazia [], depois acrescenta cada número dentro de um ciclo.

---

`nums = []`, lê n, depois `for _ in range(n): nums.append(int(input()))`.
Por fim `print(nums)`.

---

n = int(input())
nums = []
for _ in range(n):
    nums.append(int(input()))
print(nums)
