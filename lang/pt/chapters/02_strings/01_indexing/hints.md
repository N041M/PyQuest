Os caracteres são numerados a partir de 0. Os parênteses retos leem um deles: s[0].

---

Guarda primeiro a entrada, depois imprime o índice 0: `s = input()` e depois `print(s[0])`.

---

s = input()
print(s[0])
