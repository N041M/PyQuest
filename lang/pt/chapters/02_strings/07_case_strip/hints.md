Os métodos chamam-se com um ponto: s.strip(), s.upper(). Devolvem cadeias novas.

---

Encadeia-os: s.strip() remove os espaços das extremidades, .upper() maiusculiza o resultado.

---

s = input()
print(s.strip().upper())
