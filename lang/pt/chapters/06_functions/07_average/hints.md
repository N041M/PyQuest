Uma média é o total dividido pela contagem -- e tens uma função nativa para
cada metade.

---

`sum(nums)` sobre `len(nums)`, com `/` (a divisão em float de 1.9).

---

def average(nums):
    return sum(nums) / len(nums)
