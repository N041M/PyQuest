Dois valores depois de um return, separados por uma vírgula, voltam como um tuplo.

---

Já tens as duas metades de 5.3: `return min(nums), max(nums)`.

---

def min_max(nums):
    return min(nums), max(nums)
