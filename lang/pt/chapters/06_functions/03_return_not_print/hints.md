Se o verificador disser que recebeu None, a tua função imprimiu em vez de devolver.

---

Constrói a string com .upper() e + "!", depois devolve-a -- sem print nenhum.

---

def shout(word):
    return word.upper() + "!"
