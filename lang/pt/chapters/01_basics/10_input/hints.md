Guarda o que `input()` devolve numa variável, depois constrói o cumprimento com +.

---

`name = input()` depois `print("Hello, " + name)`. O texto antes do nome
inclui a vírgula e um espaço: "Hello, ".

---

name = input()
print("Hello, " + name)
