Mantém uma lista no carrinho (`self.items = []` em `__init__`); `add`
acrescenta-lhe itens.

---

`total` soma os preços -- `sum(price for name, price in self.items)` se
guardares pares `(name, price)`.
