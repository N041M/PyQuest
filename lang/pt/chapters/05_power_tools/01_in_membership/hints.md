`letter in word` já é True ou False -- coloca-o diretamente num `if`.

---

Lê a palavra, lê a letra, depois ramifica: `if letter in word:` imprime yes,
`else:` imprime no.

---

word = input()
letter = input()
if letter in word:
    print("yes")
else:
    print("no")
