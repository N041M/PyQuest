# PyQuest translation worksheet -- EXAMPLE (abbreviated).
#
# This is a short sample showing the format. Generate the REAL, complete
# worksheet -- every UI string and every puzzle's brief/hints/reference,
# English prefilled -- for your language with:
#
#     python3 tools/lang_worksheet.py new <code>
#
# Then translate the text UNDER each "#@ pyquest:<kind> <id>" marker, replacing the
# English in place, and split it into a pack with:
#
#     python3 tools/lang_worksheet.py apply <code>
#
# `apply` writes only the entries you changed; everything untouched stays
# English. Keep each file's markdown structure and ``` code blocks exactly --
# only the prose is localized, the grader is language-agnostic. Lines above the
# first marker (like these) are ignored.
#
# Below: the pack name, the UI strings, and ONE content file as a sample. The
# real worksheet continues with a `file` marker for every puzzle.
#@ pyquest:meta name
Your language's own name -- replace this (e.g. Cestina)
#@ pyquest:string menu.learn
learn
#@ pyquest:string menu.play
play
#@ pyquest:string menu.setup
set up
#@ pyquest:file 01_basics/01_hello/brief.md
# 1.1 -- Hello, output

## Concept

A **program** is a list of instructions the computer runs from top to bottom.
The most basic instruction is to **print** -- to put a line of text on the
screen. In Python you do that with `print(...)`. Whatever you put inside the
parentheses, in quotes, is shown.

`print` is a **function**: a built-in action you trigger by writing its name
followed by parentheses. The thing in quotes is **text** (Python calls text a
*string* -- a string of characters).

## Example

```python
print("Good morning")
```

When this runs, the screen shows:

```
Good morning
```

The quotes mark where the text starts and ends; they are not printed. Python
also adds a line break at the end automatically, so the next print starts on a
new line.

## Your task

Make the program print exactly this line:

```
Hello, output
```

Open the chapter workspace `work.py`, write one `print(...)` that produces that
line, **save the file**, then run `check`.

## Done when

- Running `check` shows CHECK PASSED.
- The output reads `Hello, output` -- same words, same comma. (The checker
  ignores capitalisation, but matching the brief exactly is good practice.)
